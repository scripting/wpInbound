# wpInbound

Implements inbound RSS for WordPress sites. Initially using this to map posts from scripting.com on daveverse site.



### How it works

Uses FeedLand sockets for reading feeds, updates WordPress sites via wpIdentity, so there isn't much code in this project. 

